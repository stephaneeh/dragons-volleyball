import { lightTheme } from "@mui/material/styles";

const lightTheme = createTheme({
  typography: {
    fontFamily: ["Roboto", "Helvetica"],
  },
  palette: {
    primary: {
      main: "#981FC2",
    },
  },
});

export default lightTheme;
